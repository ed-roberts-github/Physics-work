Code to run a collision simulation. 
Note to run the simulation you'll need the gnuplot package and will need to pipe output of compiled collide.cc in a file called 'data' then run by command 'gnuplot gnu12'
